services:
  - type: web
    name: finance-tracker
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn finance_app.wsgi:application
    pythonVersion: 3.10.13
    envVars:
      - key: DJANGO_SETTINGS_MODULE
        value: finance_app.settings
